{
  "name": "ex-games-living-map",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && tsc -p tsconfig.edge.json && vite build",
    "preview": "vite preview",
    "postbuild": "cp node_modules/maplibre-gl/dist/maplibre-gl-worker.mjs dist/assets/ && cp node_modules/maplibre-gl/dist/maplibre-gl-shared.mjs dist/assets/ && cp node_modules/maplibre-gl/dist/maplibre-gl.mjs dist/assets/"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.2.0"
  },
  "dependencies": {
    "maplibre-gl": "^6.1.0"
  }
}
